[package]
name        = "gently-codegen"
version     = "1.0.0"
edition     = "2021"
description = "CODIE symbol DAG → Rust source code generator"

[[bin]]
name = "gen"
path = "src/main.rs"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
